/**
 * Camada de Infraestrutura — Persistência.
 *
 * <p>Implementações concretas (adaptadores) das interfaces de repositório do
 * domínio, usando a tecnologia escolhida (JPA, JDBC, MongoDB etc.), além de
 * entidades de mapeamento e mappers entre modelo persistente e de domínio.</p>
 */
package jose.eduardo.desafio.infrastructure.persistence;
